
on:
  push:
    branches: 
      - main
  workflow_dispatch:


jobs:
  windows-test-job:
    runs-on: windows-latest

    steps:
      - run: echo "Hello"

  linux-test-job:
    runs-on: ubuntu-latest
    
    steps:
      - run: echo "Hello"

      - run: echo "${{ github.ref_name }}"
      