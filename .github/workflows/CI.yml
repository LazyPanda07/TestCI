name: CI

on:
  push:
    branches: [ "master" ]
    
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/epicgames/unreal-engine:dev-5.3.2
    steps:
      - uses: actions/checkout@v3

      - name: Print
        run: ls /home
      
