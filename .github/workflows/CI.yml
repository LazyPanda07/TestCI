
on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Variable
      run: |
        mkdir Debug_Windows
        mkdir DebugDLL_Windows
        export TEST_VARIABLE=$(ls | grep Windows)
        echo ${TEST_VARIABLE}
