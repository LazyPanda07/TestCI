
on:
  push:
    branches: [master]
  workflow_dispatch:


jobs:
  test:
    runs-on: ubuntu-latest
    container:
      # image: ghcr.io/epicgames/unreal-engine:dev-slim-5.5
      image: ghcr.io/lazypanda07/web_framework_dev:1.0.7
    
    steps:
    - uses: actions/checkout@v4

    - name: Print Unreal Engine version
      run: ls /usr/bin
