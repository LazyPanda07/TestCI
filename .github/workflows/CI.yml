name: CI

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  gcc:
    runs-on: ubuntu-latest

    steps:
      - name: Get GCC version
        run: echo $(gcc --version)

      - name: Get G++ version
        run: echo $(g++ --version)
