
on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Variable
      run: |
        mkdir Debug_Windows
        mkdir DebugDLL_Windows
        echo "TEST_VARIABLE=$(ls | grep Windows)" >> $GITHUB_ENV

    - name: Print
      run: echo ${{ env.TEST_VARIABLE }}
